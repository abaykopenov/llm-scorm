# LLM → SCORM → Chamilo Pipeline

Автоматизированный конвейер создания e-learning курсов: от генерации контента через LLM до загрузки SCORM-пакета в Chamilo LMS.

## 🚀 Возможности

- **🖥 Desktop приложение** — нативное окно Windows, без браузера
- **🌐 Web интерфейс** — открывается в браузере, доступен по сети
- **⌨️ CLI** — командная строка для автоматизации
- **Генерация курсов через LLM** — OpenAI API или локальные модели (Ollama, LM Studio, vLLM)
- **Автозагрузка в Chamilo** — SCORM автоматически загружается в LMS
- **SCORM 1.2 пакеты** — совместимы с Chamilo и другими LMS
- **Современный интерфейс** — тёмная тема, анимации, предпросмотр курса

## 📦 Установка

```bash
git clone https://github.com/abaykopenov/llm-scorm.git
cd llm-scorm
pip install -r requirements.txt
copy .env.example .env       # настройки (один раз)
```

## 🎯 Три способа запуска

### 🖥 Desktop приложение (рекомендуется)

Нативное окно Windows — весь интерфейс в одном окне:

```bash
python desktop_app.py
```

### 🌐 Web интерфейс

Открывается в браузере, доступен другим компьютерам в сети:

```bash
python web_app.py
# → http://localhost:5000
```

### ⌨️ CLI (командная строка)

Для скриптов и автоматизации:

```bash
python main.py --topic "Основы Python" --upload
```


## ⚙️ Быстрая настройка (.env)

Скопируйте `.env.example` → `.env` и укажите ваши данные:

```ini
# LLM сервер (где запущена ИИ модель)
OPENAI_BASE_URL=http://192.168.1.100:11434/v1
OPENAI_MODEL=llama3

# Chamilo LMS (куда загружать курсы)
CHAMILO_URL=http://192.168.1.50/chamilo
CHAMILO_USER=admin
CHAMILO_PASSWORD=ваш_пароль
```

> После настройки `.env` вам достаточно указывать только тему!

---

## 🛠 Варианты запуска

### Таблица всех вариантов

| Что хотите сделать | Команда |
|---|---|
| Сгенерировать SCORM по теме | `python main.py --topic "Основы Python"` |
| Сгенерировать + загрузить в Chamilo | `python main.py --topic "Основы Python" --upload` |
| Из готового JSON | `python main.py --input examples/sample_course.json` |
| JSON + загрузить в Chamilo | `python main.py --input data.json --upload` |
| Больше страниц | `python main.py --topic "Docker" --pages 5` |
| На английском языке | `python main.py --topic "Machine Learning" --lang en` |
| Указать выходной файл | `python main.py --topic "Git" --output my_course.zip` |

---

### 1. Генерация курса по теме (самый простой)

```bash
# Настройки LLM берутся из .env
python main.py --topic "Основы кибербезопасности"
```

### 2. Генерация + автозагрузка в Chamilo

```bash
# Настройки LLM и Chamilo из .env
python main.py --topic "Docker для начинающих" --upload
```

### 3. Из готового JSON

```bash
python main.py --input examples/sample_course.json
python main.py --input examples/sample_course.json --output my_course.zip
```

### 4. Через OpenAI API

```bash
export OPENAI_API_KEY="sk-..."
python main.py --topic "Основы Python" --pages 5
```

### 5. Через локальные модели (Ollama, LM Studio, vLLM)

```bash
# Ollama на другом компьютере
python main.py --topic "Docker" --base-url http://192.168.1.100:11434/v1 --model llama3

# LM Studio
python main.py --topic "SQL" --base-url http://192.168.1.100:1234/v1 --model local-model

# vLLM
python main.py --topic "ML" --base-url http://192.168.1.100:8000/v1 --model mistral

# Ollama локально
python main.py --topic "Git" --base-url http://localhost:11434/v1 --model llama3
```

> **Примечание:** Для локальных моделей API key не нужен — достаточно `--base-url`.

### 6. Полный пайплайн с ручными параметрами

```bash
python main.py \
    --topic "Основы SQL" \
    --base-url http://192.168.1.100:11434/v1 \
    --model llama3 \
    --pages 4 \
    --lang ru \
    --upload \
    --chamilo-url http://192.168.1.50/chamilo \
    --chamilo-user admin \
    --chamilo-pass secret
```

---

### Все параметры CLI

```
python main.py --help

Источник контента (обязательно — одно из двух):
  --input, -i FILE      JSON-файл с содержимым курса
  --topic, -t ТЕМА      Тема курса — ИИ сгенерирует автоматически

LLM (генерация контента):
  --base-url URL        URL сервера LLM (http://192.168.1.100:11434/v1)
  --model MODEL         Модель LLM (llama3, mistral, gpt-4o-mini...)
  --api-key KEY         OpenAI API ключ (для облачных моделей)
  --pages, -p N         Количество страниц (по умолчанию: 3)
  --lang, -l {ru,en}    Язык курса (по умолчанию: ru)

SCORM:
  --output, -o FILE     Путь для сохранения ZIP

Chamilo LMS (автозагрузка):
  --upload              Загрузить SCORM в Chamilo после сборки
  --chamilo-url URL     URL Chamilo (http://192.168.1.50/chamilo)
  --chamilo-user USER   Логин администратора (по умолчанию: admin)
  --chamilo-pass PASS   Пароль администратора
  --chamilo-course CODE Код курса в Chamilo
```

## 📁 Структура проекта

```
├── desktop_app.py             # 🖥 Desktop приложение
├── web_app.py                 # 🌐 Web интерфейс (Flask)
├── main.py                    # ⌨️ CLI — командная строка
├── llm_generator.py           # Генерация JSON через LLM
├── scorm_builder.py           # Сборка SCORM 1.2 пакета
├── chamilo_uploader.py        # Автозагрузка в Chamilo
├── config.py                  # Конфигурация
├── .env.example               # Шаблон настроек
├── requirements.txt           # Зависимости Python
├── static/                    # Интерфейс (HTML/CSS/JS)
│   ├── index.html             # SPA интерфейс
│   ├── style.css              # Стили (тёмная тема)
│   └── app.js                 # Логика UI
├── templates/                 # Шаблоны SCORM-курса
│   ├── index.html             # HTML-шаблон (Jinja2)
│   ├── style.css              # Стили курса
│   └── scorm_api.js           # SCORM 1.2 API обёртка
├── examples/                  # Примеры
│   └── sample_course.json     # Пример JSON курса
├── output/                    # Сгенерированные SCORM-пакеты
├── install_chamilo.sh         # Скрипт установки Chamilo LMS
└── README.md
```

## 📄 Формат JSON курса

```json
{
    "title": "Название курса",
    "description": "Описание",
    "language": "ru",
    "pages": [
        {
            "title": "Название страницы",
            "blocks": [
                {
                    "type": "text",
                    "title": "Заголовок",
                    "body": "<p>HTML-контент</p>"
                },
                {
                    "type": "mcq",
                    "title": "Вопрос",
                    "body": "Текст вопроса",
                    "options": [
                        {"text": "Вариант 1", "correct": true},
                        {"text": "Вариант 2", "correct": false}
                    ],
                    "feedback_correct": "Правильно!",
                    "feedback_incorrect": "Неправильно."
                },
                {
                    "type": "truefalse",
                    "title": "Верно или неверно",
                    "body": "Утверждение",
                    "correct_answer": true,
                    "feedback_correct": "Верно!",
                    "feedback_incorrect": "Неверно."
                }
            ]
        }
    ]
}
```

## 🏫 Chamilo LMS

### Автоматическая загрузка

```bash
python main.py --topic "Любая тема" --upload
```

### Ручная загрузка

1. Откройте Chamilo LMS
2. Перейдите в **Курс → Learning Path**
3. Нажмите **Import SCORM**
4. Загрузите ZIP-файл из папки `output/`

### Установка Chamilo

Для установки Chamilo на Debian/Ubuntu:

```bash
sudo bash install_chamilo.sh
```

Подробнее в [README_Chamilo_Installer.md](README_Chamilo_Installer.md).

## ⚙️ Переменные окружения

| Переменная | Описание | По умолчанию |
|---|---|---|
| `OPENAI_API_KEY` | API ключ OpenAI | — |
| `OPENAI_BASE_URL` | URL сервера LLM (для локальных моделей) | — |
| `OPENAI_MODEL` | Модель LLM | `gpt-4o-mini` |
| `OPENAI_TEMPERATURE` | Температура генерации | `0.7` |
| `OPENAI_MAX_TOKENS` | Макс. токенов | `4096` |
| `CHAMILO_URL` | URL Chamilo LMS | — |
| `CHAMILO_USER` | Логин администратора | `admin` |
| `CHAMILO_PASSWORD` | Пароль администратора | — |

## 📝 Лицензия

Свободное использование для образовательных и коммерческих проектов.
