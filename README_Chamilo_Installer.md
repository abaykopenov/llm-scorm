# Chamilo LMS — Автоматический установщик

Полностью автоматическая установка Chamilo LMS 1.11 на Debian / Ubuntu.

## 🚀 Быстрый старт

```bash
sudo bash install_chamilo.sh
```

**Всё.** Скрипт сделает всё сам:
1. Установит Apache, MariaDB, PHP 8.1
2. Создаст базу данных и пользователя
3. Скачает и распакует Chamilo
4. Создаст `configuration.php` (конфиг БД)
5. Импортирует схему базы данных
6. Создаст администратора
7. Настроит Apache VirtualHost и PHP

## ⚙️ Настройки

Все параметры в начале файла `install_chamilo.sh`:

```bash
# Версия Chamilo
CHAMILO_VER="1.11.26"

# База данных
DB_NAME="chamilo_db"
DB_USER="chamilo_user"
DB_PASS="StrongPassword123!"     # ← ИЗМЕНИТЕ

# Администратор Chamilo
ADMIN_LOGIN="admin"
ADMIN_PASS="admin123"            # ← ИЗМЕНИТЕ
ADMIN_EMAIL="admin@example.com"

# Язык
PLATFORM_LANGUAGE="russian"
```

## 📋 Результат установки

После завершения скрипт выведет:

```
  Chamilo LMS: http://192.168.1.50/chamilo/

  Администратор:
    Логин:    admin
    Пароль:   admin123

  База данных:
    Host:     localhost
    DB:       chamilo_db
    User:     chamilo_user
    Pass:     StrongPassword123!
```

## 🔧 Если Chamilo просит завершить установку через браузер

Откройте `http://ВАШ_IP/chamilo/main/install/` и используйте данные БД:

| Поле | Значение |
|---|---|
| Host | `localhost` |
| Port | `3306` |
| User | `chamilo_user` (НЕ root!) |
| Password | `StrongPassword123!` |
| Database | `chamilo_db` |

> ⚠️ **Важно:** Не используйте `root` — MariaDB на Debian/Ubuntu блокирует root для TCP-подключений (ошибка 1698).

## 🐛 Частые ошибки

### `Access denied for user 'root'@'localhost'` (Ошибка 1698)

MariaDB на Debian/Ubuntu использует `unix_socket` аутентификацию для root. 
Решение: используйте `chamilo_user` вместо `root`.

### Chamilo показывает белую страницу

```bash
sudo tail -f /var/log/apache2/error.log
```

Скорее всего не хватает PHP-расширения. Установите:
```bash
sudo apt install php8.1-xml php8.1-mbstring php8.1-gd php8.1-intl
sudo systemctl restart apache2
```

## 📋 Системные требования

- **ОС:** Debian 11/12, Ubuntu 20.04/22.04/24.04
- **RAM:** минимум 1 ГБ
- **Диск:** минимум 2 ГБ свободного места
- **Права:** root (sudo)
